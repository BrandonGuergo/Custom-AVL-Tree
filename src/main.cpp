#include "AVL.h"
#include <iostream>
#include <string>
#include <sstream>
#include <iomanip>
using namespace std;

//CANNOT let users access memory in main!
// work in the order of insert(), traverse(), search(), remove
int main(){

  //std::string numCommands;
  // Node* one; // Remove if not used
  //getline(std::cin, numCommands);
  //for (int i = 0; i < std::stoi(numCommands); i++) {
   // tree.insert("Brandon", "00000000");
  //}
  return 0;
}